<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Thin (100) -->
  <font
    android:fontStyle="normal"
    android:fontWeight="100"
    android:font="@font/paperlogy_1_thin" />
  
  <!-- Extra Light (200) -->
  <font
    android:fontStyle="normal"
    android:fontWeight="200"
    android:font="@font/paperlogy_2_extralight" />
  
  <!-- Light (300) -->
  <font
    android:fontStyle="normal"
    android:fontWeight="300"
    android:font="@font/paperlogy_3_light" />
  
  <!-- Regular (400) -->
  <font
    android:fontStyle="normal"
    android:fontWeight="400"
    android:font="@font/paperlogy_4_regular" />
  
  <!-- Medium (500) -->
  <font
    android:fontStyle="normal"
    android:fontWeight="500"
    android:font="@font/paperlogy_5_medium" />
  
  <!-- Semi Bold (600) -->
  <font
    android:fontStyle="normal"
    android:fontWeight="600"
    android:font="@font/paperlogy_6_semibold" />
  
  <!-- Bold (700) -->
  <font
    android:fontStyle="normal"
    android:fontWeight="700"
    android:font="@font/paperlogy_7_bold" />
  
  <!-- Extra Bold (800) -->
  <font
    android:fontStyle="normal"
    android:fontWeight="800"
    android:font="@font/paperlogy_8_extrabold" />
  
  <!-- Black (900) -->
  <font
    android:fontStyle="normal"
    android:fontWeight="900"
    android:font="@font/paperlogy_9_black" />
</font-family> 